{
  "name": "nmaviz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3021",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 3021",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
